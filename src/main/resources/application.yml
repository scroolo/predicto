server:
  port: 8080

spring:
  datasource:
    url: ${DATASOURCE_URL:jdbc:postgresql://localhost:5432/predicto}
    username: ${DATASOURCE_USERNAME:predicto}
    password: ${DATASOURCE_PASSWORD:predicto}
    hikari:
      maximum-pool-size: 10

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false

  redis:
    host: ${REDIS_HOST:localhost}
    port: ${REDIS_PORT:6379}
    timeout: 2000ms
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0

cito:
  api:
    mock-enabled: true
    base-url: https://api.citoapi.com/api/v1
    api-key: ${CITO_API_KEY:}
  sync:
    catalog-interval-ms: 43200000
    schedule-interval-ms: 1800000
    result-interval-ms: 300000
    lock-interval-ms: 60000

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always
